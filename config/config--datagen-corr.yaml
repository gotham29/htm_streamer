features:
  seq1:
    type: cat
    weight: 1
    min:
    max:
    resolution:
  seq2:
    type: cat
    weight: 1
    min:
    max:
    resolution:

models_state:
  model_for_each_feature: true
  return_pred_count: no
  use_sp: true

timesteps_stop:
  learning:
  running:
  sampling:
